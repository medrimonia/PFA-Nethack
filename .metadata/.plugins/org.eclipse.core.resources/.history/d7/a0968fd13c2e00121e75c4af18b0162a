package bot;

import java.util.HashMap;

public enum Variables {
	MAP("MAP"),
	MAP_HEIGHT("MAP_HEIGHT"),
	MAP_WIDTH("MAP_WIDTH"),
	ACTION_RESULT("ACTION_RESULT"),
	DUNGEON_LEVEL("DUNGEON_LEVEL"),
	UNKNOWN("?");
	
	private String token;
	private static HashMap<String,Variables> myVariables;
	
	Variables(String token){
		this.token = token;
	}
	
	static{
		myVariables = new HashMap<String,Variables>();
		for (Variables v : Variables.values()){
			myVariables.put(v.token, v);
		}
	}
	
	public static Variables tokenToVariables(String token){
		if (myVariables.containsKey(token))
			return myVariables.get(token);
		return UNKNOWN;
	}

}
